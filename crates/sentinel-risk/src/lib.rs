//! Health, candidates, market metrics. **Empty skeleton — implemented in
//! Phase 8.** Will call `aegis-math` rather than reimplement economic
//! arithmetic (`docs/architecture.md` §5, CI grep guard `CI-NOMATHDUP`);
//! that dependency is not declared yet, matching Phase 1's non-scope.
