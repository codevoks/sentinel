/**
 * The Aegis liquidation keeper loop — a thin runner over
 * packages/executor. Empty skeleton — implemented in Phase 11
 * (docs/keeper-design.md, frozen). Not begun in Phase 1 even though
 * Aegis's artifacts now exist upstream (docs/project-status.md upstream
 * reconciliation, 2026-09-18) — AGENTS.md §5 still requires exactly one
 * phase per session.
 */

export const SENTINEL_KEEPER_APP_VERSION = "0.1.0" as const;
