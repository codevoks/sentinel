/**
 * REST + WebSocket API. Empty skeleton — implemented in Phase 9
 * (docs/api-design.md, frozen). No API routes exist yet
 * (docs/phases/phase-01-foundation.md §2, explicit non-scope).
 */

export const SENTINEL_API_APP_VERSION = "0.1.0" as const;
