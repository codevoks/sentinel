/**
 * The Next.js UI. Empty skeleton — implemented in Phase 15 (docs/ui.md).
 * Next.js itself is not yet a dependency: adding a framework before there
 * is UI code to run it would be scaffolding for a future phase
 * (docs/phases/phase-01-foundation.md §2).
 */

export const SENTINEL_WEB_APP_VERSION = "0.1.0" as const;
