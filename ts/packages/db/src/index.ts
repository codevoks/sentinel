/**
 * Generated types + query layer for the tables TS reads/writes.
 * Empty skeleton — implemented starting Phase 2 (docs/architecture.md §4),
 * once the data model this package would generate types from exists.
 */

export const SENTINEL_DB_PACKAGE_VERSION = "0.1.0" as const;
