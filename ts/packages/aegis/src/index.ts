/**
 * Thin wrapper over @aegis/sdk; PDA vectors; version pinning.
 * Empty skeleton — implemented in Phase 11. Not begun in Phase 1 even
 * though Aegis's `@aegis/sdk` now exists upstream
 * (docs/project-status.md upstream reconciliation, 2026-09-18) —
 * AGENTS.md §5 still requires exactly one phase per session.
 */

export const SENTINEL_AEGIS_PACKAGE_VERSION = "0.1.0" as const;
