/**
 * Intent claim, planner, simulator, signer client, submitter, tracker.
 * Empty skeleton — implemented in Phase 10 (docs/architecture.md §4).
 */

export const SENTINEL_EXECUTOR_PACKAGE_VERSION = "0.1.0" as const;
