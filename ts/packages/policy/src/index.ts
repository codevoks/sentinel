/**
 * Signing policy engine: program/instruction allowlist, account pins, caps.
 * Empty skeleton — implemented in Phase 10 (docs/signer-and-key-management.md).
 */

export const SENTINEL_POLICY_PACKAGE_VERSION = "0.1.0" as const;
